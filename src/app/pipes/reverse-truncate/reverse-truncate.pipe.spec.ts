import { ReverseTruncatePipe } from './reverse-truncate.pipe';

describe('ReverseTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
